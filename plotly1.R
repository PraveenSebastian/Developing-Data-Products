
# plotly ------------------------------------------------------------------

#install.packages("plotly")
library(plotly)
data(mtcars)
plot_ly(mtcars, x = mtcars$wt, y =mpg, mode = "markers")

#plotly 2nd lecture

#Save the file in R mark down

#plotly 3rd lecture
